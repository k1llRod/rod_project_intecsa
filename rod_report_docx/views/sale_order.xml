<odoo>
    <data>
        <record id="view_order_form_inherit" model="ir.ui.view">
            <field name="name">sale.order.form.inherit.word.report</field>
            <field name="model">sale.order</field>
            <field name="inherit_id" ref="sale.view_order_form"/>
            <field name="arch" type="xml">
                <xpath expr="//header" position="inside">
                    <button name="%(action_generate_word_report)d" string="Orden de entrega" type="action" class="btn-primary" invisible="state != 'sale'"/>
                </xpath>
            </field>
        </record>

        <record id="action_generate_word_report" model="ir.actions.server">
            <field name="name">Generate Word Report</field>
            <field name="model_id" ref="sale.model_sale_order"/>
            <field name="binding_model_id" ref="sale.model_sale_order"/>
            <field name="state">code</field>
            <field name="code">action = {
                    'type': 'ir.actions.act_url',
                    'url': '/sale_order/report/%s' % record.id,
                    'target': 'self',
                }</field>
        </record>

    </data>
</odoo>